import type{Metadata}from"next";import Breadcrumbs from"./Breadcrumbs";import SiteHeader from"./SiteHeader";import SiteFooter from"./SiteFooter";export function legalMetadata(title:string,path:string):Metadata{return{title:title+" | Aquabon",description:"Información legal de Aquabon Lavandería.",alternates:{canonical:path}}}export default function LegalPage({title,path,children}:{title:string;path:string;children:React.ReactNode}){return <main><SiteHeader/><Breadcrumbs items={[{label:"Inicio",href:"/"},{label:title,href:path}]}/><article className="legal-page"><h1>{title}</h1>{children}<p>Última actualización: 14 de septiembre de 2026.</p></article><SiteFooter/></main>}